<% @subject = "[#{@event.project.name}] #{@format.truncatePlaintext(@event.story.text)} (##{@event.story.ref}) was blocked" %>

<%- @render('header') %>
<%- @format.user(@action.user) %> blocked the story in the <%- @format.phase(@event.phase) %> phase.
<%- @render('footer') %>
