[<a href="<%=@format.projectLink(@event.project)%>"><%=@event.project.name%></a>] Changeset (<a href="<%= @event.changeset.url %>"><%- @event.changeset.guid.substr(0,7) %></a>) was committed by <strong><%= if @event.changeset.creator? then @event.changeset.creator.name else @event.changeset.creatorName %></strong>: <em><%- @format.changesetMessage(@event.project.id, @event.changeset.message) %></em>